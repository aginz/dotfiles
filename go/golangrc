if [[ $OS == "Darwin" ]]; then
  export GOPATH="$HOME/go"
else
  alias go='go.exe'
  export GOPATH="/mnt/c/Users/leona/go"
fi

export GOBIN="$GOPATH/bin"
export PATH=$GOBIN:$PATH

alias gosrc='cd ~/go/src'
alias gobin='cd ~/go/bin'
